## Defaults for the python3 role.
##
python3_tmpdir: "{{ tmpdir_path }}/python3" 
python3_version: 3.7
python3_command: "python{{ python3_version }}"
python3_bashrc: "{{ ansible_user_dir}}/.bash_profile"
python3_local: "{{ ansible_user_dir}}/.local/bin"
python3_pyenv: null
